# Full development environment (default GPU CUDA 11.8):
# - Runtime dependencies (GPU cu118 variant)
# - Build tooling
#
# For CPU dev, use:
# python -m pip install -r requirements-dev-cpu.txt

-r requirements-gpu-cu118.txt
-r requirements-build.txt
